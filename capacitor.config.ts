import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devdactic.gmail',
  appName: 'devdacticGmail',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
